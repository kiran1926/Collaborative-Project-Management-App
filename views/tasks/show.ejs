<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <!-- Bootstrap CSS -->
     <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Task Details</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Add your CSS file -->
</head>
<body>
    <div class="container">
        <h1>Task Details of  <%= project.name %></h1>
        <div class="task-card">
            <h2><%= task.title %></h2>
            <p><strong>Description:</strong> <%= task.description %></p>
            <p><strong>Status:</strong> <%= task.status %></p>
            <p><strong>Priority:</strong> <%= task.priority %></p>
            <p><strong>Assigned To:</strong> <%= task.assignedTo ? task.assignedTo.name : 'Unassigned' %></p>
            <p><strong>Due Date:</strong> <%= task.dueDate ? task.dueDate.toDateString() : 'No due date' %></p>

            <div class="actions">
                <div class="mt-2">
                <a href="/users/<%=user._id%>/projects/<%= project._id %>/tasks/<%= task._id %>/edit" class="btn btn-secondary">Edit</a>
            </div>
            <div class="mt-2">
                <form action="/users/<%=user._id%>/projects/<%= project._id %>/tasks/<%= task._id %>?_method=DELETE" method="POST" style="display:inline;">
                    <button type="submit" class="btn btn-secondary">Delete</button>
                </form>
            </div>
            <div class="mt-2">
                <a href="/users/<%=user._id%>/projects/<%=project._id %>" class="btn btn-secondary">Back to Project</a>
            </div>
            </div>
        </div>
    </div>
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>